import {NextRequest,NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';
const products={starter:{monthly:{id:'73c8f809-3d0b-4976-9ccf-6b8d2b69f362',amount:4900,months:1},annual:{id:'f2c709b7-b79e-4762-adbc-4996bb91199d',amount:53900,months:12}},pro:{monthly:{id:'14ff1299-91b3-41fe-93e1-c378b8bf6e01',amount:14900,months:1},annual:{id:'3cc5a39e-b51b-4de2-a07c-c672f88c4561',amount:163900,months:12}},business:{monthly:{id:'9f54571a-64ba-4e3f-9db9-acb3fe1cbe38',amount:29900,months:1},annual:{id:'00632f66-5485-44a6-b65a-be4c4528237b',amount:328900,months:12}}} as const;const planCodes={starter:'static',pro:'interactive',business:'linkhub'} as const;const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL||'https://rifjsvbbhsnpifgooenl.supabase.co';const SUPABASE_KEY=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||'sb_publishable_5A_EpEK4Jrwh-3-NT43RxA_0iIP9Tdl';
function normalizePaymentKey(value:string|undefined){let key=(value||'').replace(/^\uFEFF/,'').trim();if((key.startsWith('"')&&key.endsWith('"'))||(key.startsWith("'")&&key.endsWith("'")))key=key.slice(1,-1).trim();return key.replace(/^Bearer\s+/i,'').replace(/[\u200B-\u200D\u2060]/g,'').trim()}
export async function POST(req:NextRequest){const key=normalizePaymentKey(process.env.MAKETOU_API_KEY);if(!key)return NextResponse.json({error:'PAYMENT_UNAVAILABLE'},{status:503});try{const auth=req.headers.get('authorization')||'',token=auth.startsWith('Bearer ')?auth.slice(7):'';if(!token)return NextResponse.json({error:'SESSION_EXPIRED',message:'Votre session a expiré.'},{status:401});const supabase=createClient(SUPABASE_URL,SUPABASE_KEY,{global:{headers:{Authorization:`Bearer ${token}`}}});const {data:{user},error:userError}=await supabase.auth.getUser(token);if(userError||!user)return NextResponse.json({error:'SESSION_EXPIRED'},{status:401});const {plan,firstName,lastName,billingPeriod='monthly'}=await req.json();if(!(plan in products)||!['monthly','annual'].includes(billingPeriod))return NextResponse.json({error:'Invalid plan'},{status:400});const p=plan as keyof typeof products,period=billingPeriod as 'monthly'|'annual',product=products[p][period];let businessId:string|undefined;const {data:owned}=await supabase.from('businesses').select('id').eq('owner_user_id',user.id).order('created_at',{ascending:true}).limit(1);businessId=owned?.[0]?.id;if(!businessId){const suffix=user.id.replace(/-/g,'').slice(0,8);const {data:created,error}=await supabase.from('businesses').insert({owner_user_id:user.id,name:'Mon entreprise',slug:`mon-entreprise-${suffix}`,business_type:'other',currency_code:'XOF',country_code:'CI'}).select('id').single();if(error||!created)return NextResponse.json({error:'Impossible de préparer votre espace entreprise.'},{status:500});businessId=created.id}const origin=(req.headers.get('origin')||process.env.NEXT_PUBLIC_APP_URL||new URL(req.url).origin).replace(/\/$/,'');const payload={productDocumentId:product.id,email:user.email||'',firstName:firstName||'Client',lastName:lastName||'Qatalink',redirectURL:`${origin}/payment/return?kind=subscription`,meta:{user_id:user.id,business_id:businessId,plan:p,plan_code:planCodes[p],billing_period:period,period_months:product.months}};const r=await fetch('https://api.maketou.net/api/v1/stores/cart/checkout',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify(payload),cache:'no-store'});const data=await r.json().catch(()=>null);if(!r.ok)return NextResponse.json({error:'Impossible de démarrer le paiement.',provider_status:r.status},{status:r.status});const quoted=Number(data?.cart?.total_price??data?.total_price??NaN);if(Number.isFinite(quoted)&&quoted!==product.amount){console.error('[Qatalink:CheckoutPriceMismatch]',{plan:p,period,expected:product.amount,provider:quoted,product_id:product.id});return NextResponse.json({error:'PAYMENT_PRICE_MISMATCH',message:'Ce nouveau tarif n’est pas encore synchronisé chez le prestataire de paiement. Aucun débit n’a été lancé.',expected_amount:product.amount,provider_amount:quoted},{status:409})}const cartId=data?.cart?.id||data?.id;if(cartId){const {error}=await supabase.from('payments').insert({business_id:businessId,plan_code:planCodes[p],provider:'maketou',provider_cart_id:cartId,amount_minor:product.amount,currency_code:'XOF',status:'pending',customer_email:user.email||null,billing_period:period,period_months:product.months,raw_provider_response:data});if(error&&error.code!=='23505')return NextResponse.json({error:'Impossible de préparer le suivi du paiement.'},{status:500})}return NextResponse.json(data,{status:r.status})}catch{return NextResponse.json({error:'Impossible de démarrer le paiement.'},{status:500})}}
