import { NextRequest, NextResponse } from 'next/server';
const OCR_WEBHOOK='https://digitaladn225.app.n8n.cloud/webhook/qatalink-ocr';
export async function POST(req:NextRequest){try{const body=await req.json();const payload={job_id:crypto.randomUUID(),schema_target:'qatalink_catalog_v1',...body,options:{extract_prices:true,extract_descriptions:true,group_into_categories:true,generate_item_image_prompts:true,normalize_spelling:true,preserve_raw_text:true,...body.options}};const r=await fetch(OCR_WEBHOOK,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),cache:'no-store'});const text=await r.text();let data:any;try{data=JSON.parse(text)}catch{data={success:false,status:'invalid_json',raw:text}}return NextResponse.json(data,{status:r.ok?200:r.status});}catch(e:any){return NextResponse.json({success:false,error:e.message},{status:500})}}
