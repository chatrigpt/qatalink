'use client';
import {useEffect,useState} from 'react';import {createPortal} from 'react-dom';import {ChevronDown,ChevronRight,LayoutDashboard,LogIn,Menu,UserPlus,X} from 'lucide-react';import {ThemeToggle} from '@/components/theme-toggle';import {LandingPricingRefresh} from '@/components/landing-pricing-refresh';import {LandingSocialProof} from '@/components/landing-social-proof';
const LINKS=[['Fonctionnalités','/#features'],['Comment ça marche','/#how'],['Commandes','/#operations'],['Tarifs','/#pricing'],['Clé-en-main','/cle-en-main']] as const;
export function LandingHeaderExperience(){const [host,setHost]=useState<Element|null>(null),[open,setOpen]=useState(false),[mobileOpen,setMobileOpen]=useState(false);useEffect(()=>{if(!['/','/cle-en-main'].includes(location.pathname))return;const apply=()=>{const header=document.querySelector('.landing-nav')||document.querySelector('.tk-nav');if(!header)return;const actions=(header.querySelector('.landing-nav-actions')||header.querySelector(':scope > div:last-child')) as Element|null;if(actions)setHost(actions);const nav=header.querySelector('.navlinks');if(nav){const existing=new Set(Array.from(nav.querySelectorAll('a')).map(a=>(a.textContent||'').trim().toLowerCase()));for(const [label,href] of LINKS){if(existing.has(label.toLowerCase()))continue;const a=document.createElement('a');a.textContent=label;a.href=href;a.dataset.qatalinkStandardLink='1';nav.appendChild(a)}}const old=actions?.querySelector('a[href^="/login"]') as HTMLElement|null;if(old)old.style.display='none'};apply();const observer=new MutationObserver(apply);observer.observe(document.body,{childList:true,subtree:true});const close=()=>{if(innerWidth>760)setMobileOpen(false)};addEventListener('resize',close);return()=>{observer.disconnect();removeEventListener('resize',close);document.querySelectorAll('[data-qatalink-standard-link]').forEach(x=>x.remove())}},[]);const menu=host?createPortal(<><div className="landing-account-menu landing-account-desktop"><button type="button" className="btn btn-ghost landing-account-trigger" onClick={()=>setOpen(v=>!v)}>Compte <ChevronDown size={15}/></button>{open&&<div className="landing-account-dropdown"><a href="/login?next=/mobile"><LogIn/>Se connecter</a><a href="/login?mode=signup&next=/mobile"><UserPlus/>S’inscrire</a><a href="/mobile"><LayoutDashboard/>Mes espaces</a></div>}</div><div className="landing-mobile-menu"><button className="landing-mobile-trigger" type="button" aria-label={mobileOpen?'Fermer le menu':'Ouvrir le menu'} onClick={()=>setMobileOpen(v=>!v)}>{mobileOpen?<X size={22}/>:<Menu size={22}/>}</button>{mobileOpen&&<div className="landing-mobile-panel"><div className="landing-mobile-theme"><span>Apparence</span><ThemeToggle/></div><nav>{LINKS.map(([label,href])=><a key={label} href={href}><span>{label}</span><ChevronRight size={17}/></a>)}</nav><div className="landing-mobile-separator"/><a className="landing-mobile-account-link" href="/login?next=/mobile"><LogIn/><span><b>Se connecter</b><small>Accéder à mes espaces</small></span></a><a className="landing-mobile-account-link" href="/login?mode=signup&next=/mobile"><UserPlus/><span><b>S’inscrire</b><small>Créer un compte gratuit</small></span></a><a className="landing-mobile-account-link" href="/mobile"><LayoutDashboard/><span><b>Mes espaces</b><small>Client, Pro, Équipe ou Livreur</small></span></a><a className="btn btn-primary landing-mobile-create" href="/create">Créer mon catalogue</a></div>}</div></>,host):null;return <><LandingPricingRefresh/><LandingSocialProof/>{menu}</>}
