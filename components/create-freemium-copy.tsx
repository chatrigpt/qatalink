'use client';
import {useEffect} from 'react';
export function CreateFreemiumCopy(){useEffect(()=>{if(location.pathname!='/create')return;const apply=()=>{document.querySelectorAll<HTMLElement>('p,small,span,b').forEach(el=>{if(el.children.length)return;const t=(el.textContent||'').trim();if(t.includes('Votre essai complet de 7 jours démarrera seulement'))el.textContent='Votre formule Free permanente s’active avec ce premier catalogue : 20 articles, 5 scans QR par jour et 10 commandes WhatsApp par jour.';else if(t.includes('Tout est ouvert pendant 7 jours'))el.textContent='Votre formule Free reste active sans limite de temps. Publiez d’abord votre catalogue, puis augmentez vos limites seulement si votre activité en a besoin.';else if(t.includes('essai est terminé'))el.textContent='Cette action demande une formule supérieure. Vous pouvez continuer à utiliser Qatalink en Free.'})};apply();const mo=new MutationObserver(apply);mo.observe(document.body,{childList:true,subtree:true,characterData:true});return()=>mo.disconnect()},[]);return null}
