'use client';
import {useEffect,useMemo,useState} from 'react';import {createPortal} from 'react-dom';import {CheckCircle2,Clock3,CreditCard} from 'lucide-react';import {createSupabaseBrowserClient} from '@/lib/supabase';
type Sub={plan_code:string;status:string;current_period_start:string|null;current_period_end:string|null};function label(v:string){return({free:'Free',static:'Starter',interactive:'Pro',linkhub:'Business'} as Record<string,string>)[v]||'Free'}function date(v:string|null){return v?new Intl.DateTimeFormat('fr-FR',{day:'2-digit',month:'long',year:'numeric'}).format(new Date(v)):'Sans date de fin'}
export function DashboardSubscriptionStatus(){const supabase=useMemo(()=>createSupabaseBrowserClient(),[]),[target,setTarget]=useState<Element|null>(null),[sub,setSub]=useState<Sub|null>(null),[businessName,setBusinessName]=useState('');useEffect(()=>{let cancelled=false;const resolve=()=>{const title=document.querySelector('.dash-v3-top h1')?.textContent||'';setTarget(title.includes('Abonnement')?document.querySelector('.dash-v3-main > .dash-section:last-child'):null)};resolve();const mo=new MutationObserver(resolve);mo.observe(document.body,{childList:true,subtree:true,characterData:true});void(async()=>{const {data:{session}}=await supabase.auth.getSession();if(!session)return;const {data:bs}=await supabase.from('businesses').select('id,name').eq('owner_user_id',session.user.id).order('created_at',{ascending:true}).limit(1);const b=bs?.[0];if(!b)return;setBusinessName(b.name||'Votre entreprise');const {data:ss}=await supabase.from('subscriptions').select('plan_code,status,current_period_start,current_period_end').eq('business_id',b.id).order('created_at',{ascending:false}).limit(1);if(!cancelled)setSub((ss?.[0]||null) as Sub|null)})();return()=>{cancelled=true;mo.disconnect()}},[supabase]);if(!target)return null;const end=sub?.current_period_end?new Date(sub.current_period_end).getTime():null,valid=!!sub&&sub.status==='active'&&(!end||end>Date.now());return createPortal(<section className="dash-card subscription-current-card"><div className="subscription-current-icon">{valid?<CheckCircle2/>:<CreditCard/>}</div><div className="subscription-current-copy"><span className="eyebrow">FORMULE EN COURS</span><h3>{sub?label(sub.plan_code):'Free'}</h3><p>{businessName}{sub?.plan_code==='free'?' · sans limite de temps':''}</p></div><div className="subscription-current-meta"><span className={'admin-status '+(valid?'active':'none')}>{valid?'Actif':'Inactif'}</span>{sub?.current_period_end&&<small><Clock3 size={13}/>Jusqu’au {date(sub.current_period_end)}</small>}</div></section>,target)}
