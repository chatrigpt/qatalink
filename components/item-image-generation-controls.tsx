'use client';

import {useEffect,useMemo,useRef,useState} from 'react';
import {ImagePlus,RefreshCw,Sparkles,Upload,WandSparkles,X} from 'lucide-react';
import {createSupabaseBrowserClient} from '@/lib/supabase';

type ItemRef={id:string;name:string;description:string|null;sort_order:number};
type Target={id:string;name:string;hasImage:boolean}|null;
type Mode='auto'|'custom'|null;
type ReferenceImage={file:File;preview:string}|null;
type StatusResult={job_id:string;item_id:string;status:'processing'|'completed'|'failed';image_url?:string;error?:string};

export function ItemImageGenerationControls(){
  const supabase=useMemo(()=>createSupabaseBrowserClient(),[]);
  const [target,setTarget]=useState<Target>(null);const [uploadTarget,setUploadTarget]=useState<Target>(null);
  const [mode,setMode]=useState<Mode>(null);const [customPrompt,setCustomPrompt]=useState('');const [referenceImage,setReferenceImage]=useState<ReferenceImage>(null);
  const [working,setWorking]=useState(false);const [error,setError]=useState('');const [feedback,setFeedback]=useState('');
  const itemsRef=useRef<ItemRef[]>([]);const decorateTimer=useRef<number|null>(null);const fileInputRef=useRef<HTMLInputElement|null>(null);const replaceInputRef=useRef<HTMLInputElement|null>(null);

  function clearReference(){setReferenceImage(current=>{if(current?.preview)URL.revokeObjectURL(current.preview);return null});if(fileInputRef.current)fileInputRef.current.value=''}
  function closeModal(){if(working)return;setTarget(null);setMode(null);setCustomPrompt('');clearReference();setError('')}

  async function resolveItems(){if(typeof window==='undefined'||window.location.pathname!=='/dashboard')return [] as ItemRef[];let catalogId=new URLSearchParams(window.location.search).get('catalog')||'';if(!catalogId){const {data:b}=await supabase.from('businesses').select('id').order('created_at',{ascending:true}).limit(1).maybeSingle();if(b?.id){const {data:c}=await supabase.from('catalogs').select('id').eq('business_id',b.id).order('created_at',{ascending:false}).limit(1).maybeSingle();catalogId=c?.id||''}}if(!catalogId)return [] as ItemRef[];const {data}=await supabase.from('items').select('id,name,description,sort_order').eq('catalog_id',catalogId).order('sort_order');itemsRef.current=(data||[]) as ItemRef[];return itemsRef.current}

  function decorateCards(items=itemsRef.current){const cards=Array.from(document.querySelectorAll<HTMLElement>('.editor-v2-grid .item-v2-card'));if(!cards.length)return;cards.forEach((card,index)=>{const item=items[index];if(!item)return;card.dataset.qatalinkItemId=item.id;const media=card.querySelector<HTMLElement>('.item-v2-media');if(!media)return;const hasImage=!!media.querySelector('img');let row=card.querySelector<HTMLElement>(':scope > .qatalink-image-action-row');if(!row){row=document.createElement('div');row.className='qatalink-image-action-row';media.insertAdjacentElement('afterend',row)}row.innerHTML='';
    const generate=document.createElement('button');generate.type='button';generate.className='qatalink-image-action-button';generate.innerHTML=hasImage?'<span aria-hidden="true">↻</span><span><b>Régénérer l’image</b><small>Créer une nouvelle version · 5 crédits</small></span>':'<span aria-hidden="true">✦</span><span><b>Générer une image</b><small>Créer une illustration professionnelle · 5 crédits</small></span>';generate.addEventListener('click',()=>{clearReference();setTarget({id:item.id,name:item.name,hasImage});setMode(null);setCustomPrompt('');setError('')});row.appendChild(generate);
    const upload=document.createElement('button');upload.type='button';upload.className='qatalink-image-action-button qatalink-image-upload-button';upload.innerHTML=`<span aria-hidden="true">↑</span><span><b>${hasImage?'Remplacer par ma photo':'Ajouter ma photo'}</b><small>Importer directement une image depuis votre téléphone</small></span>`;upload.addEventListener('click',()=>{setUploadTarget({id:item.id,name:item.name,hasImage});setTimeout(()=>replaceInputRef.current?.click(),0)});row.appendChild(upload);
  })}
  async function refreshDecorations(){const items=await resolveItems();decorateCards(items)}
  useEffect(()=>{if(typeof window==='undefined'||window.location.pathname!=='/dashboard')return;void refreshDecorations();const observer=new MutationObserver(()=>{if(decorateTimer.current)window.clearTimeout(decorateTimer.current);decorateTimer.current=window.setTimeout(()=>void refreshDecorations(),100)});observer.observe(document.body,{childList:true,subtree:true});const onPop=()=>void refreshDecorations();window.addEventListener('popstate',onPop);return()=>{observer.disconnect();window.removeEventListener('popstate',onPop);if(decorateTimer.current)window.clearTimeout(decorateTimer.current);document.querySelectorAll('.qatalink-image-action-row').forEach(el=>el.remove());clearReference()}},[]);

  async function uploadReplacement(file?:File){if(!file||!uploadTarget)return;if(!file.type.startsWith('image/')){setFeedback('Choisissez une image JPG, PNG ou WebP.');return}if(file.size>15*1024*1024){setFeedback('Cette image dépasse 15 Mo.');return}try{setFeedback('Import de votre image…');const {data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Session expirée.');const safe=file.name.replace(/[^a-zA-Z0-9._-]+/g,'-');const path=`${session.user.id}/${uploadTarget.id}/${Date.now()}-${safe}`;const up=await supabase.storage.from('catalog-assets').upload(path,file,{contentType:file.type||'image/jpeg'});if(up.error)throw up.error;const {data:u}=supabase.storage.from('catalog-assets').getPublicUrl(path);await supabase.from('item_images').update({is_primary:false}).eq('item_id',uploadTarget.id).eq('is_primary',true);const {error:insertError}=await supabase.from('item_images').insert({item_id:uploadTarget.id,image_url:u.publicUrl,storage_path:path,alt_text:uploadTarget.name,is_primary:true,source:'uploaded',generation_status:'completed'});if(insertError)throw insertError;applyCompletedImage(uploadTarget.id,u.publicUrl);setFeedback(uploadTarget.hasImage?'Image remplacée avec succès.':'Image ajoutée avec succès.')}catch(e:any){setFeedback(e?.message||'Impossible d’importer cette image.')}finally{setUploadTarget(null);if(replaceInputRef.current)replaceInputRef.current.value='';window.setTimeout(()=>setFeedback(''),4200)}}

  function chooseReference(file?:File){if(!file)return;if(!file.type.startsWith('image/')){setError('Choisissez un fichier image.');return}if(file.size>12*1024*1024){setError('L’image de référence doit faire moins de 12 Mo.');return}clearReference();setReferenceImage({file,preview:URL.createObjectURL(file)});setError('')}
  function updateCreditDisplay(balance:unknown){if(balance===null||balance===undefined)return;const value=String(Number(balance));const pill=document.querySelector<HTMLElement>('.credit-pill strong');if(pill)pill.textContent=value}
  function applyCompletedImage(itemId:string,imageUrl:string){const card=document.querySelector<HTMLElement>(`.item-v2-card[data-qatalink-item-id="${itemId}"]`);if(!card)return;const media=card.querySelector<HTMLElement>('.item-v2-media');if(!media)return;let image=media.querySelector<HTMLImageElement>('img');if(!image){image=document.createElement('img');image.alt=itemsRef.current.find(i=>i.id===itemId)?.name||'Illustration';const placeholder=media.querySelector('svg');if(placeholder)placeholder.replaceWith(image);else media.prepend(image)}image.src=imageUrl;decorateCards(itemsRef.current)}
  async function pollJobs(jobIds:string[],itemId:string,token:string){let pending=[...jobIds];let attempts=0;while(pending.length&&attempts<100){attempts++;await new Promise(resolve=>window.setTimeout(resolve,3000));const r=await fetch('/api/images/status',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({job_ids:pending})});const data=await r.json().catch(()=>null);if(!r.ok)continue;const results=(data?.results||[]) as StatusResult[];const completed=results.find(x=>x.status==='completed'&&x.item_id===itemId&&x.image_url);if(completed?.image_url){applyCompletedImage(itemId,completed.image_url);setFeedback('Votre nouvelle illustration est prête.');window.setTimeout(()=>setFeedback(''),4200)}if(results.some(x=>x.status==='failed')){setFeedback('Une génération a échoué. Les crédits concernés sont recrédités lorsqu’ils ont été débités.');window.setTimeout(()=>setFeedback(''),5200)}pending=results.filter(x=>x.status==='processing').map(x=>x.job_id);localStorage.setItem('qatalink_pending_image_jobs',JSON.stringify(pending))}if(!pending.length)localStorage.removeItem('qatalink_pending_image_jobs')}
  async function uploadReference(token:string){if(!target||!referenceImage)return null;const form=new FormData();form.set('item_id',target.id);form.set('file',referenceImage.file);const response=await fetch('/api/images/reference',{method:'POST',headers:{Authorization:`Bearer ${token}`},body:form});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data?.message||'Impossible d’envoyer l’image de référence.');return {url:String(data.reference_image_url||''),storagePath:String(data.reference_storage_path||'')}}
  async function launch(){if(!target||!mode||working)return;if(mode==='custom'&&!customPrompt.trim()){setError('Décrivez simplement l’image que vous souhaitez obtenir.');return}setWorking(true);setError('');try{const {data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Votre session a expiré. Reconnectez-vous puis réessayez.');const reference=mode==='custom'?await uploadReference(session.access_token):null;const r=await fetch('/api/images/generate',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({item_ids:[target.id],generation_mode:mode,custom_prompt:mode==='custom'?customPrompt.trim():undefined,reference_image_url:reference?.url||undefined,reference_storage_path:reference?.storagePath||undefined})});const data=await r.json().catch(()=>({}));if(!r.ok){if(data?.error==='INSUFFICIENT_CREDITS')setError(`Cette image demande 5 crédits. Votre solde actuel est de ${Number(data?.balance||0)}.`);else setError(data?.message||'Impossible de lancer la génération pour le moment.');return}updateCreditDisplay(data?.balance);const jobs=(data?.jobs||[]).map((j:any)=>j?.job_id).filter(Boolean) as string[];if(!jobs.length){setError('La génération n’a pas pu démarrer. Réessayez dans un instant.');return}const previous=JSON.parse(localStorage.getItem('qatalink_pending_image_jobs')||'[]');const merged=[...new Set([...(Array.isArray(previous)?previous:[]),...jobs])];localStorage.setItem('qatalink_pending_image_jobs',JSON.stringify(merged));const itemId=target.id;const usedReference=!!referenceImage;setTarget(null);setMode(null);setCustomPrompt('');clearReference();setFeedback(usedReference?'Illustration guidée par votre image en cours de création…':target.hasImage?'Nouvelle version en cours de création…':'Illustration en cours de création…');window.setTimeout(()=>setFeedback(''),3500);void pollJobs(jobs,itemId,session.access_token)}catch(e:any){setError(e?.message||'Impossible de lancer la génération pour le moment.')}finally{setWorking(false)}}

  if(typeof window!=='undefined'&&window.location.pathname!=='/dashboard')return null;
  return <><input ref={replaceInputRef} type="file" accept="image/jpeg,image/png,image/webp,image/avif" hidden onChange={e=>void uploadReplacement(e.target.files?.[0])}/>{feedback&&<div className="qatalink-image-feedback" role="status"><Sparkles size={17}/><span>{feedback}</span><button onClick={()=>setFeedback('')} aria-label="Fermer"><X size={14}/></button></div>}{target&&<div className="qatalink-image-modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)closeModal()}}><section className="qatalink-image-modal" role="dialog" aria-modal="true"><button className="qatalink-image-modal-close" onClick={closeModal}><X size={18}/></button><div className="qatalink-image-modal-icon">{target.hasImage?<RefreshCw size={23}/>:<ImagePlus size={23}/>}</div><span className="eyebrow">ILLUSTRATION QATALINK</span><h2>{target.hasImage?'Comment voulez-vous recréer cette image ?':'Comment voulez-vous créer cette image ?'}</h2><p className="qatalink-image-modal-lead">Pour « {target.name} », choisissez la méthode la plus simple pour vous.</p><div className="qatalink-generation-choices"><button type="button" className={mode==='auto'?'selected':''} onClick={()=>{setMode('auto');clearReference();setError('')}}><span className="qatalink-generation-choice-icon"><Sparkles size={20}/></span><span><b>Laisser Qatalink créer l’image</b><small>Qatalink utilise le nom et la description de l’article.</small></span></button><button type="button" className={mode==='custom'?'selected':''} onClick={()=>{setMode('custom');setError('')}}><span className="qatalink-generation-choice-icon"><WandSparkles size={20}/></span><span><b>Décrire l’image que je veux</b><small>Ajoutez vos instructions et éventuellement une image de référence.</small></span></button></div>{mode==='custom'&&<div className="qatalink-custom-image-prompt"><label>Décrivez l’image souhaitée</label><textarea rows={5} maxLength={4000} autoFocus value={customPrompt} onChange={e=>setCustomPrompt(e.target.value)}/><div className="qatalink-reference-image-block">{referenceImage?<button type="button" className="qatalink-reference-preview" onClick={()=>fileInputRef.current?.click()}><img src={referenceImage.preview} alt="Référence"/><span><RefreshCw size={16}/>Changer l’image</span></button>:<button type="button" className="qatalink-reference-upload" onClick={()=>fileInputRef.current?.click()}><Upload size={20}/><span><b>Ajouter une image de référence</b><small>Optionnel</small></span></button>}<input ref={fileInputRef} type="file" accept="image/jpeg,image/png,image/webp,image/avif" hidden onChange={e=>chooseReference(e.target.files?.[0])}/></div></div>}{error&&<div className="qatalink-image-error">{error}</div>}<div className="qatalink-image-modal-actions"><button className="btn btn-ghost" onClick={closeModal}>Annuler</button><button className="btn btn-primary" onClick={launch} disabled={!mode||working||(mode==='custom'&&!customPrompt.trim())}>{working?'Lancement…':target.hasImage?'Régénérer l’image · 5 crédits':'Générer l’image · 5 crédits'}</button></div></section></div>}</>;
}
